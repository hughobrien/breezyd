# Twinfresh "Breezy" param map (unit type 0x0011)

**Source device:** `192.168.1.148` (`BREEZY00000000A0`, playroom)
**Method:** Live read/write via `tools/probe.py` with operator observation.
**Date:** 2026-05-03

## Device-to-location mapping (orthogonal to params, useful for config)

| IP            | Device ID          | Location | Protocol pwd |
|---------------|--------------------|----------|--------------|
| 192.168.1.148 | BREEZY00000000A0   | playroom | `testpwd`   |
| 192.168.1.152 | BREEZY00000000A1   | bedroom  | `testpwd`   |
| 192.168.1.160 | BREEZY00000000A2   | office   | `testpwd`   |

(Factory default is `1111`. User changed all three to `testpwd` during Phase 0 on 2026-05-03.)

## Parameter table

| id    | name                | type   | unit | observed_behavior                                        | safe_to_write | notes |
|-------|---------------------|--------|------|----------------------------------------------------------|---------------|-------|
| 0x01  | power               | uint8  | -    | 0=off (fan stops), 1=on. Confirmed by toggling on .148 and .152 — operator heard the unit shut off. | yes | |
| 0x02  | speed_mode          | uint8  | -    | **Mode selector + preset.** Write 1/2/3 to select low/med/high preset (audibly confirmed). Write `0xFF` to engage manual-percentage mode (the actual percentage comes from 0x44). Writes 4..254 to 0x02 are clamped to preset 3. Reads return the active preset (1/2/3) or `0xFF` when in manual mode. | yes | |
| 0x44  | speed_manual_pct    | uint8  | %    | **Manual percentage (10..100).** Confirmed: writes update the value but do NOT switch into manual mode by themselves — also write `0x02 = 0xFF`. **Device firmware clamps to a 10% floor**: writing 5 stored as 10. Operator confirmed the app shows the value set here (e.g. wrote 23 → app showed "23%"). | yes | To run at N% manual: write `0x44 = N` (N ≥ 10), then write `0x02 = 0xFF`. To return to preset: write `0x02 = 1/2/3`. The Go client should reject writes below 10 with a typed error rather than silently letting the device clamp. |
| 0x07  | (airflow_mode?)     | uint8  | -    | TBD — possibly airflow mode (1=ventilation, 2=heat-recovery, 3=air-supply per Atmo); also see 0xB7 | TBD | |
| 0xB7  | (airflow_mode_alt?) | uint8  | -    | TBD                                                      | TBD           | |
| 0x1A  | fan_target_rpm?     | uint16 | rpm? | Read 800 in sweep AND when fan was running at 100% manual. If this were live RPM it should change with speed; doesn't. Likely target/max RPM, a fan-speed setpoint, or updates only on a slow cadence. Treat as TBD until a longer observation window confirms semantics. | no | |
| 0x1F  | temp_outdoor_intake | uint16 | 0.1°C | Confirmed: read 218 ↔ app shows "outside air in 21.8 °C". One of four temp sensors around the heat exchanger. uint16 LE in tenths of °C. | no | |
| 0x20  | temp_indoor_supply  | uint16 | 0.1°C | Confirmed: read 225 ↔ app shows "indoor air out 22.5 °C". | no | air entering the indoor space (post heat exchange) |
| 0x21  | temp_indoor_intake  | uint16 | 0.1°C | Confirmed: read 219 ↔ app shows "indoor air in 21.9 °C". | no | air leaving the indoor space (return) |
| 0x22  | temp_outdoor_exhaust| uint16 | 0.1°C | Confirmed: read 220 ↔ matches the second "outside air" reading 21.7 °C with drift. | no | air leaving to outside (post heat exchange) |
| 0x24  | filter_timer        | uint16 | ?    | Countdown (decremented 3350 → 3339 over ~2h on 2026-05-03). Units unclear; not cleanly hours or minutes. Likely a vendor-specific counter against a service interval. | no | filter-replacement timer; units unconfirmed |
| 0x25  | humidity            | uint8  | %    | Confirmed: read 54 → 53 → 52 over ~3h, matches app's "52% RH". | no | |
| 0x27  | co2_or_eco2         | uint16 | ppm? | Read values 1175 → 3395 → 3512 over the session, matching app's "CO2" reading (e.g. 3400-3651). Operator notes the values seem high vs typical indoor CO2 (400-1000 ppm), suspecting sensor calibration. The Vents app calls this CO2 but the actual sensor may be eCO2 or VOC-derived. | no | name conservatively as `co2_ppm` in v1 with a comment about possible miscalibration |
| 0x3A  | temperature_a_int   | uint8  | °C   | Single-byte rounded mirror of one of the 0x1F-0x22 readings (22 ≈ 21.8 or 21.9). Possibly redundant with the high-resolution 4-temp set. | no | preferred reads use 0x1F-0x22 |
| 0x3B  | temperature_b_int   | uint8  | °C   | Single-byte rounded mirror; 23 ≈ 22.5. | no | |
| 0x7C  | device_id_search    | ascii  | -    | 16-char device ID (response to discovery query)          | no            | |
| 0x7D  | protocol_password   | ascii  | -    | 4 chars; default "1111"                                  | NEVER         | leaks plaintext to any LAN client |
| 0x95  | wifi_ssid           | ascii  | -    | SSID                                                     | NEVER         | |
| 0x96  | wifi_password       | ascii  | -    | WiFi password                                            | NEVER         | leaks plaintext |
| 0x9C  | ip_ap_mode          | ipv4   | -    | 192.168.4.1 (typical AP fallback)                        | NEVER         | |
| 0x9D  | subnet_mask         | ipv4   | -    | 255.255.255.0                                            | NEVER         | |
| 0x9E  | gateway             | ipv4   | -    | -                                                        | NEVER         | |
| 0x9F  | dns                 | ipv4   | -    | 8.8.8.8                                                  | NEVER         | |
| 0xA3  | ip_address          | ipv4   | -    | 192.168.1.148 — the device's own DHCP-assigned IP        | NEVER         | |
| 0xB9  | unit_type           | uint16 | -    | 0x0011 (17) = "Breezy"                                   | no            | read-only identifier |

## Protocol-level findings (independent of param semantics)

- **Auth failure response:** When the password is wrong, the device replies with function `0x07` and a 2-byte payload `01 <byte>`. When the password is correct, function is `0x06`. The Go client should treat function `0x07` as `ErrAuth` rather than passing it through.
- **Discovery is unauthenticated:** Sending any request with `DEFAULT_DEVICEID` returns the device's true 16-char ID and unit type *regardless of password*. This means discovery and password-recovery via search both work even when you don't know the protocol password.
- **High-byte page mechanism:** The protocol's `0xFF` byte in a payload prefixes a "high byte" for the next param ID, allowing access to params at addresses `0xHH00..0xHHFB`. To read VOC at `0x0320`, send payload `FF 03 20`. Pages confirmed populated on Breezy (unit type 0x0011): page 0x00 (~50 params, the main set), page 0x03 (~9 params including VOC at 0x0320), page 0x04 (~10 params, appear to be config/calibration values). Pages 0x01, 0x02, 0x05 returned no useful content.

## High-page params (page 0x03 and 0x04)

| id      | name              | type   | unit  | observed_behavior                               | safe_to_write | notes |
|---------|-------------------|--------|-------|-------------------------------------------------|---------------|-------|
| 0x0302  | unknown_p3_02     | uint16 | ?     | 2048 (`0x0800`)                                 | no (TBD)      | possibly a calibration/threshold |
| 0x0303  | unknown_p3_03     | uint16 | ?     | 1024 (`0x0400`)                                 | no (TBD)      | |
| 0x0306  | unknown_p3_06     | uint8  | -     | 1                                               | no (TBD)      | |
| 0x030B  | unknown_p3_0B     | uint8  | -     | 0                                               | no (TBD)      | |
| 0x030C  | unknown_p3_0C     | uint8  | -     | 1                                               | no (TBD)      | |
| 0x0315  | unknown_p3_15     | uint8  | -     | 1                                               | no (TBD)      | |
| 0x031F  | unknown_p3_1F     | uint16 | ?     | 100                                             | no (TBD)      | possibly a temp setpoint or % threshold |
| **0x0320** | **voc_index**  | uint16 | -     | **Confirmed:** read 343 then 350; matches app's VOC values (338-350 range). Vendor-typical VOC index scale (1-500). | no | **The VOC sensor reading.** Updates slowly (~1/min). |
| 0x0329  | unknown_p3_29     | uint8  | -     | 1                                               | no (TBD)      | |
| 0x0400-0x0409 | various     | uint8/16 | -   | Page-4 has 10 readable params: mostly small uint8 (0/1/2/100) plus a couple uint16. Appear to be defaults or per-mode configuration; semantics not characterized. | no (TBD) | enumerate via Phase-0 follow-up if needed |

## In-progress notes

- Phase 0 will continue: characterize 0x02 (speed) by writing 1/2/3 and listening; characterize 0x07 vs 0xB7 by writing each and observing what changes; sweep remaining IDs from `2026-05-03-paramsweep-148-raw.txt` for static-during-session vs dynamic.
- Tests run on .148 only. Bedroom (.152) and office (.160) untouched after location identification.
